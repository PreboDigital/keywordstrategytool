# Railway / Nixpacks config for Keyword Strategy Tool
[phases.install]
cmds = ["python -m pip install -r requirements.txt"]

[start]
cmd = "streamlit run app.py --server.port $PORT --server.address 0.0.0.0 --server.headless true"
